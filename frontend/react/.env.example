# Optional.
# For the no-domain AWS EC2 deployment, leave these unset so the app uses same-origin URLs:
#   API: /api
#   Socket.IO: current browser origin
# Set these only if you later host the frontend and backend on different origins.
VITE_API_URL=
VITE_SOCKET_URL=
